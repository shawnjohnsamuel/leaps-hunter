"""v7 deterministic engine (ADR 0010): config, sources, and (in later phases)
macro/patterns/gates/optmodel/sizing/portfolio. Standard library only — no
third-party dependencies — so the engine runs identically on the desktop and
in the cloud routine sandbox, which has no pip/setup step as of Phase 0.
"""
